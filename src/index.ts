import testSnippets, { components, dependencies, Snippet, TagActions, TagActionConfig } from './testSnippets';
import testSnippetsCommand from './testSnippetsCommand';

export default testSnippets;
export { components, dependencies, Snippet, TagActions, TagActionConfig, testSnippetsCommand };
